name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest test_app.py -v || true

      - name: Build Docker image
        run: docker build -t simple-web-cicd:latest .

      - name: Save Docker image
        run: |
          docker save -o app.tar simple-web-cicd:latest
          chmod 644 app.tar

      - name: Copy image to server via SCP (using password)
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          source: "app.tar"
          target: "/home/ubuntu/"
          overwrite: true
          debug: true

      - name: Deploy container on server via SSH (using password)
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          script: |
            cd /home/ubuntu
            sudo docker load -i app.tar
            sudo docker stop simple-web-app || true
            sudo docker rm simple-web-app || true
            sudo docker run -d --name simple-web-app -p 80:8080 simple-web-cicd:latest
            sudo docker ps